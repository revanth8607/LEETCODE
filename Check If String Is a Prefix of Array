class Solution {
    public boolean isPrefixString(String s, String[] words) {
        String str = "";
        boolean t = false;
        for(int i=0;i<words.length;i++){
            str = str + words[i];
            if(str.equals(s)){
                t = true;
                break;
            }
        }
        return t;
    }
}
